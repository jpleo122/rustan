mod map;
mod coordinates;
mod board;
mod game;
mod actions;

fn main() {

    // Entry point to start a game right now

    // from settings and playcount, initialize a game, initialize a game loop, and start the game loop
}

